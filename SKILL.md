---
name: birthday-reminder-gift
description: 生日提醒与礼物推荐自动化技能。管理人物画像（农历生日、性别、年龄、爱好、家人/朋友/伴侣分类及家庭角色）；仅需1个每月1日定时任务，当天若为1月1日自动先做开年流程（农历换算、更新年龄星座、重置去重标记），再扫描全量画像，对未来60天内过生日且今年未推送过的人批量推送礼物建议与行动提醒（去重，每人每年只推一次）。适用于：设置生日提醒、维护生日人物画像、为家人/朋友/伴侣挑选生日礼物、开年生日规划、农历生日换算、生日礼物清单推荐等场景。
---

# 生日提醒与礼物推荐（Birthday Reminder & Gift）

## 概述

基于人物画像数据的年度生日管理与礼物推荐系统。画像数据由使用者**私有维护**（JSON 文件），本技能只提供框架、模板与执行流程，打包分发不含任何真实画像数据。

## 使用前提

- **画像数据文件**：JSON 格式，路径由使用者在初始化时指定（默认 `birthday_profiles.json`，置于使用者项目目录）。
- **登录态依赖**：小红书搜索需要云端浏览器登录态（首次使用需登录，过期需重登）。
- **双通道推送**：飞书消息 + 邮箱。邮箱经 Agent Mail（QQ 邮箱 Agent）用使用者已绑定账户代发，收件地址在使用者初始化时配置；两通道可单独开关。
- **定时任务**：依赖 doubao-cron-scheduler 定时任务体系，创建/更新任务前必须先读该技能。

## 流程导航（按触发场景选择对应文档）

| 场景 | 阅读 |
|---|---|
| 使用者版使用说明（如何开始、FAQ） | `references/user-guide.md` |
| 使用示例（月度扫描完整输出长什么样） | `references/usage-example.md` |
| 初始化（新使用者建档、填模板、建月度扫描任务） | `references/workflow.md` §1 |
| 开年任务触发（每年 1 月 1 日） | `references/workflow.md` §2 |
| 月度扫描触发（每月 1 号） | `references/workflow.md` §3 |
| 画像字段定义与校验规则 | `references/data-schema.md` |
| 农历→公历换算（含闰月） | `references/lunar-conversion.md` |
| 礼物双层检索与 5 备选输出 | `references/gift-search.md` |

## 核心规则

1. **数据与逻辑分离**：画像只存 JSON 文件，绝不写入定时任务 query；分发时只发本技能框架，不带真实数据。
2. **生日默认农历**：`zodiac_sign` 用出生年首个公历日期（月日）推算、终身不变；`age` 与 `annual_birthday` 每年开年更新。
3. **礼物必须双层**：外部检索（小红书优先）+ 人物代入推演；输出固定 5 个备选，每个理由必须引用画像具体信息（爱好/角色/避坑备注/预算）。
4. **分类影响沟通与送礼**：伴侣重心意、家人重实用、朋友重新奇；家人须用 `family_role`（妈妈/爸爸等）细化。
5. **定时任务纪律**：先读 doubao-cron-scheduler 技能再调用定时工具；不向用户展示任务 ID；执行时间与实际推送时间一致。

## 快捷资源

- `assets/profile-template.json`：空白画像模板，新使用者填写用。
- `assets/sample-profile.json`：脱敏示例（化名+虚构爱好），演示与结构参考用，可删。
- `scripts/lunar_conversion.py`：农历换算脚本（优先本地库，不可用则提示走搜索兜底）。
